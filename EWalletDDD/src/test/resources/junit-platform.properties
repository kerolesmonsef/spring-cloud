# @RequiredArgsConstructor test classes (@SpringBootTest) inject through the constructor.
# Must live here, not application.properties — it's read before the Spring context loads.
spring.test.constructor.autowire.mode = all
